Working with the server
  Login
    ssh idaang@128.84.52.141
    password: 1234
    ssh ida@192.168.1.163
  Logout
    exit
  Transferring file to server from any computer (if you are within the
  correct folder of the file you want to transfer)
    scp -r name.py idaang@128.84.52.141:/home/idaang/
    Password: your personal login
  Transferring from personal laptop to workstation
    scp -r name.py ida@192.168.1.157:/home/ida/Desktop/

nohup mpirun -np # python3 __.py --par1 # --par2 # > _.log 2> _.err < /dev/null &

Connecting to Personal laptop from workstation
  On same network
    On personal laptop type ifconfig | grep inet to find IP address
    sftp idaang@xx.xx.x.xxx
    Navigate using lls, lcd etc
    Put file from workstation to laptop using
      put directory_name/
      put file.* or *.txt
    Get file from laptop using
      get file.*

Connecting to personal workstation
  If on the same network
    ssh idaang@cornell.local
  Alternatively
    ssh ida@xxx.xxx.x.xxx
Connecting to personal laptop
  ssh idaang@xxx.xxx.x.xxx
  Transfer to Desktop
    scp -r name.ext idaang@192.168.1.157:/Users/idaang/Desktop/

General Update to System Packages
  sudo apt-get update
  sudo apt-get dist upgrade

Checks
  Check for size of a directory (git has a 50 mb limit)
    du -sh path/
  Check IP address, look for inet
    Linux: ifconfig -a
    Mac: ifconfig | grep inet
  Check FEniCS version
    dolfin-version
  Check Ubuntu version
    lsb_release -a

Dropbox
  Check status
    dropbox status
  Syncing issues
    stop dropbox
    start dropbox

Git Commands
  git pull origin main
  git add .
  git commit -am "message"
  git push origin main

  For some cases
    git pull origin master
    git push origin master

Git Clone Repo
  sudo apt install git
  Login into Github
  By the code button, copy the link
  git clone https://github.com/idaang267/etc.git
  Type in username and Token

Install GMSH
  sudo apt install gmsh
Open with GUI
  gmsh name.geo
Convert to msh file then xml file
  gmsh name.geo -format msh2 -3
  dolfin-convert name.msh name.xml

Install .deb software
  Navigate to downloads folder/folder the deb file resides
  Sudo apt install ./name.deb

FEniCS specific commands
  Parallel processing
    mpirun -np #NumberThreadsDesired python3 name.py
  Running parameters through command line
    python3 name.py --ParameterName #ParameterValue

Linux MATLAB Installation
  Unzip the folder
  Navigate into folder and type sudo ./install
  When it asks for username use username of system NOT NetID
  When it says "click here", drag that into a browser
  Authenticate with cornell email
  Need to add to .bashrc

Multiphenics Installation
  git clone https://gitlab.com/multiphenics/multiphenics.git
  Navigate to multiphenics folder
  python3 setup.py install

sudo apt install python3-pip
pip3 install scipy
pip3 install matplotlib
pip3 install meshio[all]
